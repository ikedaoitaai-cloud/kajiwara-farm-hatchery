# 孵化率予測システム 操作マニュアル

カジワラファーム様向け

**バージョン**: 2.0
**最終更新**: 2026-06-28

---

## 📱 はじめに

このシステムは、入卵データから孵化するメス鶏の数を予測します。
PC・タブレット・スマートフォンのブラウザで使用できます。インストールは不要です。

**アクセス先**
https://ikedaoitaai-cloud.github.io/kajiwara-farm-hatchery/

このページをブックマークしておくと便利です。

---

## 🏠 トップ画面

システムを開くと、最初にこの画面が表示されます。

```
        今日は何をしますか？

  ┌──────────────┐   ┌──────────────┐
  │      🧮      │   │      📋      │
  │  入卵数の計算 │   │  孵化実績入力 │
  │              │   │              │
  │  [計算する]   │   │[実績を入力する]│
  └──────────────┘   └──────────────┘
```

日々の作業はこの2つだけです。

- **🧮 入卵数の計算** … これから入卵する分の孵化予測を出します
- **📋 孵化実績入力** … 孵化した後、実際の羽数を記録します

画面の右上には、補助的なボタンが並んでいます。

- **📊 記録数：○件** … これまでに保存した記録の件数
- **⚙️ 管理** … 計算モデルの切り替えと、計算方法の説明
- **📤 出力** … CSV出力・バックアップ・復元

---

## 🧮 入卵数の計算

### ステップ1：入卵日を確認する

画面を開くと、今日の日付が入卵日として入っています。
別の日にする場合は「📅 変更」ボタンを押してカレンダーから選んでください。

### ステップ2：農場のデータを入力する

**農場名**

「農場A」〜「農場E」のボタンから選びます。
一覧にない農場は、下の入力欄に直接名前を入力してください。

**親鳥日齢**

よく使う日齢はボタンで選べます。

| ボタン | 実際に使われる値 |
|---|---|
| 170〜200日 | 185日 |
| 230〜250日 | 240日 ← 孵化率のピーク |
| 260〜350日 | 305日 |
| 430日以上 | 430日 |

正確な日齢がわかっている場合は、下の入力欄に直接数字を入れてください（例：240）。
直接入力したほうが予測は正確になります。

**入卵数**

孵卵器に入れる卵の個数です（例：10000）。

**貯卵日数**

産卵から入卵までに何日置いたかです。初期値は7日です。

⚠️ 10日を超えると孵化率が下がります。17日以上では4割減として計算されます。

### ステップ3：予測を確認する

**入力すると同時に、画面右側の予測が自動で更新されます。**
「計算実行」のようなボタンを押す必要はありません。

紫色のパネルに表示される大きな数字が **予測メス鶏数** です。これがお客様への納品羽数の目安になります。

その下に、参考値として総孵化数と孵化率が並びます。

### ステップ4：農場を追加する（複数農場の場合）

「＋ 農場を追加する」を押すと、農場カードが増えます。
農場は何件でも追加でき、**全農場の合計が自動で計算されます**。

間違えて追加した場合は、カード右上の「× 削除」で消せます。

### ステップ5：結果を保存する

紫のパネルの下にある「この結果を保存する」を押すと、記録として残ります。
画面下に「✓ 記録として保存しました」と表示されれば完了です。

※ 農場名・日齢・入卵数の3つが入っていない農場は保存されません。

---

## 📋 孵化実績入力

孵化が終わったら、実際の羽数を記録します。**この入力が、予測精度を上げていくための一番大切な作業です。**

### ステップ1：カレンダーから日付を選ぶ

画面左側にカレンダーが表示されます。入卵記録のある日付に色がついています。

| 色 | 意味 |
|---|---|
| 🟡 黄色 | 記録あり・実績が未入力または一部のみ |
| 🟢 緑色 | 全農場の実績入力が完了 |
| 🟣 紫色 | いま選択している日 |

**黄色の日付を探して押してください。** それが実績待ちの記録です。

「‹」「›」ボタンで前の月・次の月に移動できます。

### ステップ2：農場ごとに実績を入力する

日付を選ぶと、画面右側にその日の農場が並びます。
農場ごとに2つの数字を入力してください。

- **実際のメス鶏数**
- **実際の総孵化数**

各農場の右上には、そのとき出した予測値が「予測：2,245羽」のように表示されているので、見比べながら入力できます。

### ステップ3：精度を確認する

メス鶏数を入力すると、**その場で予測精度が表示されます。**

```
予測精度（メス鶏数で比較）
┌──────────┬──────────┐
│  区分線形  │  正規分布  │
│  +150羽   │  +212羽   │
│  誤差 6.7% │  誤差 9.4% │
└──────────┴──────────┘
```

2つの計算モデルのどちらが当たっていたかが並んで表示されます。
緑色は実績が予測を上回った、赤色は下回ったという意味です。

### ステップ4：保存する

「実績を保存する」を押してください。
カレンダーの色が緑に変われば、その日の入力は完了です。

---

## ⚙️ 管理画面

計算に使うモデルを切り替える画面です。同時に、**システムがどういう計算をしているかの説明書**にもなっています。

### 計算モデルの選択

**📐 区分線形モデル（推奨）**

カジワラファーム様の経験に基づいて作った、実用的なモデルです。
親鳥の日齢を段階で区切り、それぞれに孵化率を設定しています。

| 日齢 | 孵化率 | 備考 |
|---|---|---|
| 165日以下 | 20% | 若い親鳥はまだ成熟していない |
| 165〜230日 | 20% → 40% | 成長期・上昇していく |
| 230〜250日 | 40% | **ピーク期・ベストな時期** |
| 250〜530日 | 40% → 35% | 成熟期・ゆっくり低下 |
| 530日以上 | 35% → 32% | 高齢期 |

**📊 非対称正規分布モデル（参考）**

区分線形をもとに、変化をなめらかな曲線で表したモデルです。
段階の境目で数字が飛ばないのが特徴ですが、**通常は区分線形をお使いください。**

### 補正係数（両モデル共通）

**🥚 貯卵日数の補正**

| 日数 | 補正 |
|---|---|
| 7日以内 | 補正なし（×1.00） |
| 8〜10日 | 5%低下（×0.95） |
| 11〜16日 | 15%低下（×0.85） |
| 17日以上 | 40%低下（×0.60） |

**🌡️ 季節補正**

| 時期 | 補正 | 理由 |
|---|---|---|
| 2〜5月・10〜12月 | 補正なし（×1.00） | — |
| 1月（正月明け） | 5%低下（×0.95） | 孵卵器の温度立ち上がりに時間がかかる |
| 6〜9月（夏期） | 8%低下（×0.92） | 暑さで卵が劣化する |

**🐔 メス鶏率：61%**（オス39%）

### 計算の流れ

```
① 農場ごとの総孵化数
   入卵数 × 基礎孵化率（日齢） × 貯卵日数補正 × 季節補正
   例：10,000個 × 40% × 1.00 × 0.92 ＝ 3,680羽
                ↓ 全農場を合計
② 総孵化数（全農場合計）
                ↓ メス鶏率をかける
③ 予測メス鶏数
   総孵化数 × 61%
   例：3,680羽 × 61% ＝ 2,245羽
```

選んだモデルは次回以降も引き継がれます。「設定を保存してホームへ戻る」で戻ってください。

---

## 📤 データ出力

⚠️ **月に一度はバックアップを取ってください。** データはブラウザの中に保存されているため、キャッシュを消すと失われます。

### 📊 CSV出力

Excelで開ける形式で記録を出力します。農場ごとに1行です。

出力される項目：
入卵日／農場名／入卵数／貯卵日数／親鳥日齢／予測メス鶏数／予測総孵化数／予測孵化率／実績メス鶏数／実績総孵化数／実績記録日／登録日時

※ 予測値は区分線形モデルのものが出力されます。

ファイル名：`孵化記録_2026-06-28.csv`

### 💾 バックアップ

すべての記録をファイル1つにまとめて保存します。**これが本当のバックアップです。**
パソコンの買い替えやブラウザの変更のときは、必ず先にこれを実行してください。

ファイル名：`孵化記録バックアップ_2026-06-28.json`

保存したファイルは、Google ドライブや USB メモリなど、パソコン以外の場所にも置いておくと安心です。

### 📂 復元

バックアップしたファイルから記録を戻します。
「ファイルを選択して復元」を押し、保存しておいた `.json` ファイルを選んでください。

現在の記録と統合されます。同じ記録が二重になることはありません。

---

## ⚠️ データについて知っておいてほしいこと

- **データはこのパソコン（ブラウザ）の中に保存されます。** サーバーには送られません
- **ブラウザのキャッシュや履歴を消すと、記録も消えます**
- **別のパソコンやスマホからは、同じ記録は見えません**
- 記録を移したいときは「バックアップ」→ 別の端末で「復元」

これらは将来のバージョンで、クラウド保存に対応する予定です。

**推奨ブラウザ**：Google Chrome / Safari / Microsoft Edge（いずれも最新版）

---

## 💡 よくある質問

**Q1. 「計算実行」ボタンが見当たりません**
数値を入れると自動で計算されます。ボタンを押す必要はありません。画面右側の紫のパネルをご覧ください。

**Q2. 複数の農場を登録できますか？**
はい、何件でも追加できます。合計も自動計算されます。

**Q3. 入力を間違えました**
保存前なら、その場で数字を直せます。農場ごと消したい場合はカード右上の「× 削除」を押してください。

**Q4. どちらの計算モデルを使えばいいですか？**
「区分線形」を使ってください。カジワラファーム様の経験をもとに作ったモデルです。

**Q5. 予測が外れました**
実績を入力していただくことが、そのまま精度改善につながります。外れた記録こそ大切なので、必ず入力してください。

**Q6. 日齢のボタンと直接入力、どちらがいいですか？**
正確な日齢がわかっているなら直接入力してください。ボタンは範囲の代表値（例：230〜250日 → 240日）で計算されます。

**Q7. カレンダーの日付が押せません**
入卵記録がある日付（色がついている日）だけが押せます。色がない日は、その日の入卵記録がありません。

**Q8. 貯卵日数が長いとどうなりますか？**
10日を超えると孵化率が下がります。17日以上では4割減として計算されます。

**Q9. 夏と冬で予測は変わりますか？**
はい。6〜9月は暑さの影響で8%低く、1月は5%低く計算されます。

**Q10. データが消えてしまいました**
バックアップファイルがあれば「📤 出力」→「復元」から戻せます。バックアップがない場合、残念ながら復元できません。**月に一度のバックアップをおすすめします。**

**Q11. 保存できませんというメッセージが出ました**
ブラウザの保存容量がいっぱいです。バックアップを取ったうえで、古い記録を整理してください。

---

## 📊 用語集

| 用語 | 説明 |
|---|---|
| 入卵 | 孵卵器に卵を入れること |
| 入卵日 | 孵卵器に卵を入れた日 |
| 貯卵日数 | 産卵から入卵までに置いた日数 |
| 親鳥日齢 | 卵を産んだ鶏の日齢（生後何日目か） |
| 孵化率 | 入卵数に対して孵化した割合 |
| メス鶏率 | 孵化したヒヨコのうちメスの割合（61%として計算） |
| 区分線形 | 経験に基づく予測式（推奨） |
| 非対称正規分布 | なめらかな曲線で表した予測式（参考） |
| 補正係数 | 貯卵日数や季節による孵化率の増減率 |

---

## 📞 サポート・お問い合わせ

操作方法がわからない、エラーが出た、こういう機能がほしい——どれでもご連絡ください。

**開発者**: So Ikeda
**連絡先**: 別途お伝えしているメールアドレス・お電話番号へご連絡ください
**対応時間**: 平日 9:00-18:00

---

**対象システム**: 孵化率予測システム v2.0（2026-06-28 時点）
