# 孵化率予測システム - プロジェクト概要

**Document Version**: 2.0
**Last Updated**: 2026-06-28
**対象実装**: commit `56b6df3`（2026-06-28）

## 📋 プロジェクト情報

**クライアント**: カジワラファーム
**目的**: 孵化率予測の精度向上とシステム化
**現状**: お母様の手計算（精度70-75%）
**目標**: システム化により精度85-90%へ改善
**公開URL**: https://ikedaoitaai-cloud.github.io/kajiwara-farm-hatchery/

## 🎯 開発フェーズ

### Phase 1: フロントエンドMVP・GitHub Pages公開（現在・ほぼ完了）
- ✅ 予測ロジックの実装（2モデル＋補正係数）
- ✅ 複数農場対応
- ✅ 実績入力・精度分析
- ✅ データ出力（CSV／バックアップ／復元）
- ✅ GitHub Pages 公開
- ⬜ カジワラファーム社へのデモ提供・フィードバック収集

### Phase 2: バックエンド開発（Week 5-9）
- 認証システム
- データベース構築（PostgreSQL推奨）
- REST API開発

### Phase 3: データ収集（1年間）
- 実運用データの蓄積（目標100-150サンプル）
- 予測精度の継続的改善

### Phase 4: ML モデル導入（1年後）
- XGBoost/LightGBMへの置き換え
- 予測精度85-90%達成

## 🏗️ 技術スタック

### フロントエンド（現行）
- React 18.x（UMD版・CDN、シングルHTMLファイル）
- Tailwind CSS（CDN・カスタムカラーパレット）
- Babel standalone 7.26.4（ブラウザ内JSX変換）
- Noto Sans JP（Google Fonts）
- LocalStorage（Phase 1のみ）

ビルド工程なし。`index.html` 単一ファイル（954行）で完結します。

### バックエンド（Phase 2以降）
- **推奨**: Supabase (PostgreSQL + 認証 + API + ストレージ)
- 代替A: Firebase (NoSQL + 認証)
- 代替B: Railway + FastAPI + PostgreSQL

### ML開発（Phase 4）
- Python 3.10+
- XGBoost / LightGBM
- pandas, numpy
- FastAPI (推論API)

## 🖥️ 画面構成

```
App（状態管理・画面ルーティング）
├── TopScreen            トップ「今日は何をしますか？」
├── CalculationScreen    入卵数の計算（リアルタイム予測）
├── HatchResultsScreen   孵化実績入力（カレンダー起点）
│   └── FarmActualCard   農場別の実績入力＋精度分析
├── AdminScreen          管理画面（モデル選択・係数説明）
└── DataExportScreen     CSV出力・バックアップ・復元

共通コンポーネント
├── NumberField          単位付き数値入力
├── PageHeader           全画面共通ヘッダー（記録数バッジ・遷移ボタン）
├── TitleBar             画面タイトル
├── Modal                確認・警告ダイアログ（alert/confirm の代替）
└── Toast                完了通知（2.5秒で自動消去）
```

### 画面フロー

```
        ┌─────────── トップ画面 ───────────┐
        │                                  │
   🧮 入卵数の計算                    📋 孵化実績入力
   ・入卵日選択                       ・カレンダーで日付選択
   ・農場データ入力（複数可）          ・農場ごとに実績入力
   ・リアルタイム予測                  ・両モデルの精度を即時表示
   ・記録として保存                    ・実績を保存
        │                                  │
        └──── ⚙️ 管理 ／ 📤 出力（ヘッダーから随時）────┘
```

3月版の「入卵日選択 → 農場入力 → 予測結果 → 履歴」という直列4画面フローは、6月の改修で上記の並列2タスク構成に置き換えました。予測は入力と同時に更新されるため、独立した「予測結果画面」は廃止しています。

## 📊 データ構造（v4）

```javascript
{
  id: 1751000000000,                  // タイムスタンプID
  recordDate: "2026-06-27T10:30:00Z", // 記録作成日時
  incubationDate: "2026-06-27",       // 入卵日

  // 農場データ（複数対応）— 予測・実績を農場単位で保持
  farms: [{
    id: 1751000000001.234,
    farmName: "農場A",
    parentAge: 240,                   // 親鳥日齢
    eggCount: 10000,                  // 入卵数
    storageDays: 7,                   // 貯卵日数

    predictions: {                    // 両モデルの予測を保持
      linear: { totalChicks: 3680, femaleChicks: 2245, hatchRate: "36.8" },
      normal: { totalChicks: 3680, femaleChicks: 2245, hatchRate: "36.8" }
    },
    actual: {                         // 後日入力
      femaleChicks: null,
      totalChicks: null,
      recordedDate: null
    }
  }],

  // 全農場合計の予測
  totalPredictions: {
    linear: { totalChicks, femaleChicks, hatchRate },
    normal: { totalChicks, femaleChicks, hatchRate }
  }
}
```

### v3 → v4 の変更点

- **予測値・実績値の保持単位をレコード単位から農場単位へ移動**。これにより農場別の精度分析が可能になりました
- レコード単位の合計予測は `totalPredictions` として別に保持します
- 起動時に `hatcheryRecordsV4` を読み、なければ `hatcheryRecordsV3` を `migrateToV4()` で変換して保存し直します

### ML用派生特徴量について

初期設計では `ageSquared`・`storageLog`・`ageStorageInteraction`・`environment.month`・`environment.season` を保存する想定でしたが、**現行実装では保存していません**。これらはすべて `parentAge`・`storageDays`・`incubationDate` から後から算出できるため、Phase 4 の前処理段階で生成する方針とします。

### LocalStorage キー

| キー | 内容 |
|---|---|
| `hatcheryRecordsV4` | 入卵記録の配列（現行） |
| `hatcheryRecordsV3` | 旧形式（読み込み時にのみ参照） |
| `calcModel` | 選択中の計算モデル（`linear` / `normal`） |

## 🧮 予測アルゴリズム詳細

### 最終計算式

```
農場ごとの総孵化数 = 入卵数 × 基礎孵化率(親鳥日齢) × 貯卵日数補正 × 季節補正
総孵化数           = 全農場の合計
予測メス鶏数       = 総孵化数 × 0.61
孵化率(表示値)     = 総孵化数 ÷ 総入卵数 × 100
```

### 区分線形モデル（推奨）

お母様の経験値ベース。理解しやすく調整が容易です。

```
f(age) = {
  20                                          (age < 165)
  20 + (40-20) × (age-165)/(230-165)         (165 ≤ age ≤ 230)
  40                                          (230 < age ≤ 250)
  40 - (40-35) × (age-250)/(530-250)         (250 < age ≤ 530)
  35 - (35-32) × min((age-530)/200, 1)       (age > 530)
}
```

- 若すぎる親鳥（165日以下）: 20%
- 成長期（165-230日）: 線形に上昇
- ピーク期（230-250日）: 40%
- 成熟期（250-530日）: 緩やかに下降
- 高齢期（530日以上）: 200日かけて32%まで下降

### 非対称正規分布モデル（参考）

生物学的モデル。滑らかな変化を表現します。

```
f(age) = 24.5 + 15.5 × exp(-0.5 × ((age-240)/σ)²)

σ = { 32  (age ≤ 240)  ← 左側急峻
      300 (age > 240)  ← 右側緩やか
```

- ピーク位置: 240日齢
- ピーク値: 40%
- 左右非対称（若い方が急激に変化）

### 補正係数

**貯卵日数補正**

| 日数 | 係数 |
|---|---|
| 7日以内 | 1.00 |
| 8-10日 | 0.95 |
| 11-16日 | 0.85 |
| 17日以上 | 0.60 |

**季節補正**

| 時期 | 係数 | 理由 |
|---|---|---|
| 2-5月・10-12月 | 1.00 | — |
| 1月（正月明け） | 0.95 | 孵卵器温度の立ち上がり時間 |
| 6-9月（夏期） | 0.92 | 暑さによる卵の劣化 |

**メス鶏率**: 0.61 固定（オス0.39）

### 精度分析の計算

```
差分   = 実績メス鶏数 − 予測メス鶏数
誤差率 = |差分 ÷ 予測メス鶏数| × 100
```

区分線形・非対称正規分布の両モデルについて同時に算出し、実績入力画面に並べて表示します。

### 主要関数

| 関数 | 役割 |
|---|---|
| `piecewiseLinear(age)` | 区分線形モデルの基礎孵化率 |
| `asymmetricNormal(age)` | 非対称正規分布モデルの基礎孵化率 |
| `storageFactor(days)` | 貯卵日数補正係数 |
| `seasonFactor(month)` | 季節補正係数 |
| `calcOneFarm(age, eggs, storageDays, month)` | 1農場の両モデル予測 |
| `calcTotals(farmResults, totalEggs)` | 全農場合計の両モデル予測 |
| `migrateToV4(record)` | v3 → v4 データ変換 |

## 🎨 UI/UX 設計思想

### 設計原則

主たる利用者はPC操作に不慣れな方であることを前提としています。

1. **1画面1タスク** — トップ画面の主要な選択肢は2つだけ
2. **大きな文字** — トップ画面の説明文19px、カード見出し30px、最重要の予測メス鶏数は58px（一般的なWebアプリの1.3〜1.5倍）
3. **広いタップ領域** — トップ画面の主要ボタンは `py-[22px] text-[22px]`（実効高さ約66px）
4. **即座のフィードバック** — 入力と同時に予測を更新、保存はトースト通知
5. **専門用語を避ける** — モデル名にも必ず平易な説明を併記
6. **ブラウザ標準ダイアログを使わない** — `alert`/`confirm` は自前のモーダルに置換

### カラーパレット

3月版のブルー／インディゴ（Apple風グラデーション）から、6月版で以下に刷新しました。

| 用途 | 色 | Tailwind名 |
|---|---|---|
| 背景 | `#FDFBF7` | `cream` |
| 主要文字 | `#1C1917` | `ink` |
| 本文 | `#57534E` | `body` |
| 補助文字 | `#A8A29E` | `faint` |
| 罫線 | `#E7E5E4` | `line` |
| ブランド（計算系） | `#D97706` | `brand` |
| セカンダリ（実績系） | `#7C3AED` | `sec` |
| 成功・完了 | `#16A34A` | `ok` |

計算に関する画面はアンバー、実績に関する画面はバイオレットで統一し、**いまどちらの作業をしているかを色で判別できる**構造にしています。

### タイポグラフィ

Noto Sans JP（400/500/600/700/900）。フォールバックは Hiragino Kaku Gothic ProN → system-ui。

### レスポンシブ

`lg:` ブレークポイントで左右分割 ⇄ 縦積みを切り替えます。計算画面・実績入力画面ともに、デスクトップでは左に入力・右に結果、タブレット／スマートフォンでは縦に並びます。

## 📤 データ出力仕様

### CSV

```
入卵日, 農場名, 入卵数, 貯卵日数, 親鳥日齢,
予測メス鶏数, 予測総孵化数, 予測孵化率%,     ← 区分線形モデルのみ
実績メス鶏数, 実績総孵化数, 実績記録日, 登録日時
```

- 農場ごとに1行（1レコードが複数行に展開されます）
- BOM付きUTF-8（Excelでの文字化け防止）
- ファイル名: `孵化記録_YYYY-MM-DD.csv`

⚠️ **既知の課題**: 非対称正規分布モデルの予測値がCSVに含まれないため、Excel上での2モデル比較ができません。Phase 3 のデータ分析で必要になるため、列の追加を検討中です。

### JSON バックアップ

```javascript
{ version: "v4", exportDate: "2026-06-28T...", records: [...] }
```

ファイル名: `孵化記録バックアップ_YYYY-MM-DD.json`

### 復元

v4／v3／配列直書きの3形式を受け付けます。v3・配列形式は `migrateToV4()` を通してから取り込みます。既存記録とはIDで重複を除いて統合し、`recordDate` の降順で並べ替えます。

## 🔧 Phase 1 残タスク

### 高優先度
- [x] モバイル・タブレット最適化
- [x] データエクスポート機能（CSV/JSON）
- [x] エラーハンドリング改善（保存失敗・容量超過の明示）
- [x] LocalStorageバックアップ機能
- [x] データインポート（復元）機能
- [ ] バリデーション強化（現状は農場名・日齢・入卵数の必須チェックのみ。日齢や貯卵日数の異常値検出は未実装）
- [ ] CSV出力に非対称正規分布モデルの予測値を追加

### 中優先度
- [ ] 印刷用CSS最適化
- [ ] PWA化検討（オフライン対応）
- [ ] グラフ表示（Chart.js導入）— 精度推移の可視化
- [ ] ビルド環境の導入（Babel standaloneの排除による初期表示の高速化）

### 低優先度
- [ ] アニメーション追加
- [ ] ダークモード
- [ ] 多言語対応

## 📁 ファイル構成

### 現行（Phase 1）

```
kajiwara-farm-hatchery/
├── index.html              # アプリ本体（954行）
├── README.md               # プロジェクト説明
├── PROJECT_OVERVIEW.md     # このファイル
└── USER_MANUAL.md          # 操作マニュアル
```

### Phase 2以降（リファクタリング後の想定）

```
kajiwara-farm-hatchery/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   ├── TopScreen.jsx
│   │   ├── CalculationScreen.jsx
│   │   ├── HatchResultsScreen.jsx
│   │   ├── AdminScreen.jsx
│   │   └── DataExportScreen.jsx
│   ├── utils/
│   │   ├── calculations.js      # 予測計算ロジック
│   │   ├── storage.js           # データ永続化
│   │   └── export.js            # CSV/JSONエクスポート
│   ├── App.jsx
│   └── index.js
├── docs/
│   ├── USER_MANUAL.md
│   └── API_SPEC.md
├── tests/
│   └── calculations.test.js
├── package.json
└── README.md
```

## 🚀 GitHub Pages デプロイ

公開済みです。`main` ブランチのルートから配信しています。

```
Settings → Pages → Source: main branch → / (root)
```

`main` に push すると数分で反映されます。ビルド工程がないため、`index.html` の変更がそのまま公開されます。

## 🐛 既知の制限事項

### Phase 1（現在）

1. **LocalStorage依存**
   - ブラウザキャッシュのクリアでデータ消失
   - ブラウザ・デバイス間でデータ共有不可
   - → 緩和策として「📤 出力」画面にバックアップ／復元を実装済み
   - → Phase 2でクラウドDB対応予定

2. **認証なし**
   - URLを知っていれば誰でもアクセス可能
   - ただしデータは各利用者のブラウザ内にのみ保存されるため、他者の記録は閲覧できません
   - → Phase 2で認証機能追加予定

3. **オフライン非対応**
   - React・Tailwind・BabelをすべてCDNから読み込むため接続必須
   - → PWA化で改善検討

4. **ブラウザ内JSXコンパイル**
   - Babel standaloneのため初回表示に若干の待ちが生じます
   - → Phase 2でビルド環境を導入して解消

5. **CSV出力が区分線形モデルのみ**
   - 2モデルの比較分析がExcel上でできません

## 📊 データ収集目標（Phase 3）

### 必要なサンプル数
- **統計的有意**: 50回以上の入卵記録
- **理想的**: 100-150回（季節変動を捉える）
- **ML学習に十分**: 200回以上

### 週次入卵頻度による年間サンプル数
- 週1回入卵 → 年間52サンプル ✓ 最低ライン
- 週2回入卵 → 年間104サンプル ✓✓ 推奨
- 週3回入卵 → 年間156サンプル ✓✓✓ 理想的

実績入力画面をカレンダー起点とし、入力状況を色分け（黄＝未入力／緑＝完了）したのは、この継続性を確保するための設計です。未入力の記録が視覚的に残るため、入力漏れに気づきやすくなっています。

### データ品質管理チェックリスト

月次レビュー:
- [ ] 入力漏れの確認（カレンダー上の黄色い日付をチェック）
- [ ] 異常値の検出（誤差率が20%以上の記録を確認）
- [ ] データの一貫性（農場名の表記ゆれ等）
- [ ] JSONバックアップの取得

四半期レビュー:
- [ ] 予測精度の推移確認（CSV出力からExcelで集計）
- [ ] 農場別・季節別の傾向分析
- [ ] 2モデルの精度比較
- [ ] ユーザーフィードバック反映状況確認

## 🤖 ML モデル開発計画（Phase 4）

### 6ヶ月時点での中間評価項目

1. **予測精度の現状把握**
   - MAPE（平均絶対誤差率）の計算
   - 区分線形 vs 非対称正規分布の精度比較

2. **特徴量の重要度分析**
   - 親鳥日齢・貯卵日数・季節要因の影響度
   - 農場間差異の有無

3. **ML導入の費用対効果試算**
   - 現状予測式との精度差
   - 改善見込み vs 開発コスト

### ML モデル候補

**レベル1（簡易・解釈性重視）**: 線形回帰＋多項式特徴量
**レベル2（推奨・バランス型）**: XGBoost / LightGBM
**レベル3（高度・精度重視）**: ニューラルネットワーク（200+サンプル必要）

### 特徴量エンジニアリング案

```python
# 基本特徴量（現行データから直接取得可能）
- parent_age          # 親鳥日齢
- egg_count           # 入卵数
- storage_days        # 貯卵日数
- month               # incubationDate から算出
- farm_name           # 農場名

# 派生特徴量（前処理で生成）
- age_squared         # 日齢の2乗
- storage_log         # log(貯卵日数+1)
- age_storage_interaction  # 日齢×貯卵日数
- season_category     # 季節カテゴリ
- is_summer           # 夏フラグ（6-9月）
- is_january          # 1月フラグ
- days_from_peak      # ピーク日齢(240)からの距離

# ラグ特徴量（時系列）
- prev_hatch_rate     # 同一農場の前回孵化率
- avg_hatch_rate_last_3  # 直近3回の平均
```

## 💰 コスト見積もり

| フェーズ | 内訳 | 月額 |
|---|---|---|
| **Phase 1（現在）** | GitHub Pages $0／ドメイン $0／開発環境 $0 | **$0** |
| Phase 2 | Supabase $0-25／Vercel $0／独自ドメイン $10-20/年 | **~$25** |
| Phase 4 | ＋ ML推論サーバー $30-50 | **~$50** |

## 🔒 セキュリティ・法的考慮事項

### データ所有権
- すべての入卵データ・実績データはカジワラファーム社に帰属
- 開発成果物（ソースコード）の利用権限は契約で明確化
- 推奨: 簡易契約書または覚書を交わす

### 秘密保持
- 孵化率データは企業機密として扱う
- 第三者への開示禁止
- NDA（秘密保持契約）締結を推奨

⚠️ **未対応**: 現時点でNDA・権利帰属の取り決めは未締結です。本格運用の前に対応が必要です。

### データバックアップ戦略

現行（Phase 1）:
- 「📤 出力」画面からの手動JSONバックアップ（月1回を推奨）
- バックアップファイルはクラウドストレージ（Google Drive等）にも保管

Phase 2以降:
- 日次自動バックアップ（3世代保持）
- 月次エクスポート（Excel/CSV形式）

## 📈 成功指標（KPI）

### Phase 1（デモ段階）
- ⬜ カジワラファーム社からのポジティブフィードバック
- ⬜ 初回デモの成功（操作性・UI評価）
- ⬜ フィードバックに基づく改善実施

### Phase 2-3（運用段階）
- データ入力完了率 > 95%
- システム稼働率 > 99%
- ユーザー満足度 > 4/5点
- 四半期レビュー実施率 100%

### Phase 4（ML導入後）
- 予測精度（MAPE） < 10%
- 現状式との精度差 +10-15%改善
- お母様の工数削減 50%以上
- 発注精度向上による経済効果測定

## 🔄 バージョン履歴

### v2.0 (2026-06-28) — commit `56b6df3`
- 🎨 UI全面リデザイン「シンプル＆大きく」（538行を書き換え、676行を削除）
- ✨ トップ画面を2択構成に整理
- ✨ リアルタイム予測（計算ボタンを廃止）
- ✨ カレンダー起点の孵化実績入力・農場別の精度分析
- ✨ 管理画面の新設（モデル選択・補正係数・計算式の全体像）
- ✨ データ出力画面の新設（CSV・バックアップ・復元）
- ✨ 独自モーダル／トースト通知
- 📝 データ構造をv4へ（予測・実績を農場単位で保持）
- ♻️ 予測表示の共通化・冗長コードの削除
- 🎨 カラーパレットをクリーム／アンバー／バイオレットへ刷新

### v1.0 (2026-03-08) — commit `dc2b420`
- 🎉 初回リリース（860行）
- ✨ 区分線形モデル・非対称正規分布モデル
- ✨ 複数農場対応・両方式の比較表示
- ✨ 基本的な入力・予測・履歴機能
- 🎨 Apple風UI（ブルー／インディゴ）

> **注**: Git管理前の2026年3月の開発過程では内部的に v2.0〜v2.3 という版数を用いていました。リポジトリ作成時に版数を振り直しているため、旧ドキュメントの版数表記とは対応しません。上記のGitコミット単位の版数を正とします。

## 📞 サポート・問い合わせ

**開発者**: So Ikeda
**GitHub**: https://github.com/ikedaoitaai-cloud/kajiwara-farm-hatchery
**問題報告・機能要望**: [GitHub Issues](https://github.com/ikedaoitaai-cloud/kajiwara-farm-hatchery/issues)
**対応時間**: 平日 9:00-18:00（緊急時除く）

※ メールでの連絡先は、カジワラファーム様へ別途お伝えしています。

## 📚 関連ドキュメント

- [README.md](./README.md) - プロジェクト概要・セットアップ
- [USER_MANUAL.md](./USER_MANUAL.md) - ユーザー向け操作マニュアル

---

**Document Author**: So Ikeda
**Last Updated**: 2026-06-28
**Document Version**: 2.0
